import React from 'react';
import { AppBar, Toolbar } from '@material-ui/core';

function TopNav () {
    return (
      <AppBar
        position="relative"
      >
        <Toolbar>

        </Toolbar>
      </AppBar>
    );
}

export default TopNav;
